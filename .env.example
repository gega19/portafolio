# Sitio estático (Vite + React): no hace falta ninguna variable de entorno
# para compilar ni para que imágenes y enlaces funcionen en producción.
#
# Si más adelante añades analíticas, mapas o APIs, usa prefijo VITE_
# y documenta cada clave aquí. Ejemplo:
# VITE_ANALYTICS_ID=
